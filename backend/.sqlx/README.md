# sqlx offline metadata

This directory is reserved for checked query metadata if compile-time
`query!`/`query_as!` macros are introduced. The current repository APIs use
typed `query_as` with integration tests against migrated PostgreSQL databases,
so there are no generated metadata files yet.
