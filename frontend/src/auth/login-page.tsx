import { Navigate } from 'react-router'; import { useAuth } from '@/auth/auth-provider'; import { Button } from '@/components/ui/button'; import { Card,CardContent,CardHeader,CardTitle } from '@/components/ui/card'
export default function Login(){const {user}=useAuth();if(user)return <Navigate to='/' replace/>;return <main className='grid min-h-dvh place-items-center bg-muted p-4'><Card className='w-full max-w-sm'><CardHeader><CardTitle className='text-2xl'>歡迎使用 Baln</CardTitle><p className='text-sm text-muted-foreground'>清楚掌握每一筆收支與帳戶餘額。</p></CardHeader><CardContent><Button className='w-full' onClick={()=>location.assign('/api/v1/auth/google/start')}>使用 Google 繼續</Button></CardContent></Card></main>}
